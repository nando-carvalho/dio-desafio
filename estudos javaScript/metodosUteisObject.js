// Object.keys (retorna as chaves)
// Object.entries()  retorna chave e valor so que em array
// Object.values - retorna os valores
// Object.freeze (congela o objeto, impedindo modificar as propriedades dos atributos de configuração)
// Object.defineProperties (define as propriedades de mais de um objeto)
// Object.defineProperty (define as propriedades de um objeto)
//  Object.assign (des, any) - copia objetos
//  Object.getOwnPropertyDescriptor(0bj, 'prop') descobre a propriedade configurada no objeto
//  ...spread - copia um objeto sem apontar para o mesmo endereço de memoria


